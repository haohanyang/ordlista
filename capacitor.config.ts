import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ordlista",
  appName: "Ordlista",
  webDir: "www",
  server: {
    androidScheme: "https",
  },
};

export default config;
